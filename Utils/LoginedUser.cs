using Azure.Core;
using Microsoft.AspNetCore.Http;
using System.Security.Claims;

namespace FurnitureERP.Utils
{
    public class LoginedUser
    {
        //public int MyProperty { get; set; }

        //public static LoginedUser GetLoginedUser(HttpRequest request)
        //{
        //    var t = request.HttpContext.User.Claims.Where(c => c.Type == ClaimTypes.GroupSid || c.Type == ClaimTypes.Name).ToList();
        //}
    }
}

